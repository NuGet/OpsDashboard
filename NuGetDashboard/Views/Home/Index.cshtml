
<script type="text/javascript"> 

    $(document).ready(function () {
        GetPingdomStatusUpdate();
        GetWorkerStatusUpdate();
        GetSLAStatusUpdate();
    });

    function ShowDetails(action, controller) {
        
      $.ajax({
          url: ' /' + controller + '/' + action ,
                      type: "GET",
                      dataType: 'html',
                      success: function (result) {
                         
                          $("#backbutton").attr('class','back-button big page-back');
                          $("#title").html(controller + "<small>" + action + "</small>");                                              
                          $("#maincontent").html(result);
                      }
      });
    
              };

</script>
  
<div class="page secondary with-sidebar">    
    <div class="page-header">
    <div class="page-header-content" id="headercontent">
        <h1 id="title"></h1>
        <a href="/" id="backbutton"></a>
    </div>
</div>    
            <div style="position:fixed; top: 1em; right: 1em;"> <span class="square fg-color-white">Updated a minute ago</span></div>
            <div >@Html.Action("Tiles", "Home");</div>
    
</div>

